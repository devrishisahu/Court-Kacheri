{
  "name": "court-kacheri-monolith",
  "version": "1.0.0",
  "description": "Monolithic deployment wrapper for Court-Kacheri",
  "private": true,
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js"
  }
}